# Deploy all API handlers directly (Vercel will auto-detect them)
# Exclude only tests and internal helpers

# Exclude tests
**/*.test.ts
**/*.test.tsx
**/*.spec.ts

# Exclude dev files
.env
.env.local
*.log
